<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="spring.model.mapper.CartMapper">
	
<select id="list" parameterType="String" resultType="CartDTO">
select c.no, c.userID, c.itemNo, c.count,c.itemSize,c.itemColor,i.image as itemPicture,i.title as itemTitle,i.price as itemPrice
from cart c, item i
where userID = #{userID} and c.itemNo = i.no
</select>


</mapper>