<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="spring.model.mapper.itemMapper">
	
<select id="list" parameterType="String" resultType="ItemDTO">
select no, image, title, description, content, price,salePrice, type
from item
where type = #{type}
</select>

<select id="read" parameterType="int" resultType="ItemDTO">
		select no, image, title, description, content, price,salePrice, type
		from item
		where no = #{no}                                           
</select>

</mapper>