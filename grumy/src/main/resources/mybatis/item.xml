<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="spring.model.mapper.itemMapper">
	
<select id="list" parameterType="String" resultType="ItemDTO">
select no, name, image, title, description, content, price,salePrice, color, sizes, count, type
from item
where type = #{type}
</select>
</mapper>